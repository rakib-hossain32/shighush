import { FileText, MessageSquareWarning, ShieldCheck } from "lucide-react";
const steps = [[FileText,"Case ID দিন","যে প্রকাশিত নথি নিয়ে আপত্তি, তার ID দিন।"],[MessageSquareWarning,"কারণ লিখুন","ভুল, privacy risk বা প্রাতিষ্ঠানিক জবাব স্পষ্ট করুন।"],[ShieldCheck,"রিভিউ হবে","মডারেশন দল নথি ও আবেদন পর্যালোচনা করবে।"]] as const;
export function AppealGuidance(){return <aside className="h-fit border border-border bg-muted p-6"><p className="mono text-[11px] font-bold tracking-[.14em] text-primary">কীভাবে কাজ করে</p><div className="mt-6 grid gap-6">{steps.map(([Icon,title,copy],index)=><div className="flex gap-3" key={title}><span className="grid size-8 shrink-0 place-items-center rounded-full bg-secondary text-sm font-bold">{index+1}</span><div><div className="flex items-center gap-2"><Icon className="size-4 text-primary"/><h2 className="font-bold">{title}</h2></div><p className="mt-1 text-sm leading-6 text-muted-foreground">{copy}</p></div></div>)}</div></aside>}
